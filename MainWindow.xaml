<!-- File: /MainWindow.xaml -->
<Window x:Class="LegendBorn.MainWindow"
        x:Name="RootWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:tabs="clr-namespace:LegendBorn.Views.Tabs"
        Title="LegendBorn"
        Icon="pack://application:,,,/Resources/Assets/logolauncher.png"
        Width="1240" Height="800"
        MinWidth="1000" MinHeight="680"
        WindowStartupLocation="CenterScreen"
        Background="#080A14"
        Foreground="#EAF0FF"
        FontFamily="Segoe UI"
        WindowStyle="None"
        ResizeMode="CanResize"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                            CornerRadius="18"
                            GlassFrameThickness="0"
                            ResizeBorderThickness="6"
                            UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <Border x:Name="RootBorder">
        <Border.Style>
            <Style TargetType="Border" BasedOn="{StaticResource RootBorderStyle}">
                <Setter Property="BorderBrush" Value="#3B2364"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=WindowState}"
                                 Value="Maximized">
                        <Setter Property="CornerRadius" Value="0"/>
                        <Setter Property="Margin" Value="0"/>
                        <Setter Property="BorderThickness" Value="0"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>

        <Grid>
            <Rectangle>
                <Rectangle.Fill>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#070815" Offset="0"/>
                        <GradientStop Color="#0B0E1A" Offset="0.52"/>
                        <GradientStop Color="#080A12" Offset="1"/>
                    </LinearGradientBrush>
                </Rectangle.Fill>
            </Rectangle>

            <Ellipse Width="620" Height="620"
                     Opacity="0.12"
                     HorizontalAlignment="Left"
                     VerticalAlignment="Top"
                     Margin="-260,-300,0,0">
                <Ellipse.Fill>
                    <RadialGradientBrush>
                        <GradientStop Color="#A855F7" Offset="0"/>
                        <GradientStop Color="#007C3AED" Offset="1"/>
                    </RadialGradientBrush>
                </Ellipse.Fill>
            </Ellipse>

            <Ellipse Width="520" Height="520"
                     Opacity="0.09"
                     HorizontalAlignment="Right"
                     VerticalAlignment="Bottom"
                     Margin="0,0,-220,-300">
                <Ellipse.Fill>
                    <RadialGradientBrush>
                        <GradientStop Color="#6D5CFF" Offset="0"/>
                        <GradientStop Color="#006D5CFF" Offset="1"/>
                    </RadialGradientBrush>
                </Ellipse.Fill>
            </Ellipse>

            <Grid Margin="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="56"/>
                    <RowDefinition Height="12"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Style="{StaticResource TopBarStyle}" Padding="12,7" CornerRadius="15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Button Grid.Column="0"
                                Style="{StaticResource BrandButtonStyle}"
                                ToolTip="Главная"
                                Command="{Binding OpenStartCommand}">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Border Width="34" Height="34"
                                        CornerRadius="11"
                                        Background="#281A46"
                                        BorderBrush="#5F3A92"
                                        BorderThickness="1">
                                    <Image Source="pack://application:,,,/Resources/Assets/logolauncher.png"
                                           Stretch="Uniform"
                                           Margin="4"
                                           RenderOptions.BitmapScalingMode="HighQuality"/>
                                </Border>

                                <StackPanel Margin="11,0,0,0" VerticalAlignment="Center">
                                    <TextBlock Text="LEGENDBORN"
                                               FontWeight="SemiBold"
                                               FontSize="12"
                                               Foreground="{StaticResource TextStrongLocal}"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                        <TextBlock Text="Launcher"
                                                   Foreground="{StaticResource TextDimLocal}"
                                                   FontSize="10"/>
                                        <Border Margin="7,0,0,0" Padding="6,1" CornerRadius="8"
                                                Background="#241638" BorderBrush="#3F285E" BorderThickness="1">
                                            <TextBlock Text="{Binding LauncherVersion}"
                                                       Foreground="#CDB9F4"
                                                       FontSize="9"/>
                                        </Border>
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </Button>

                        <Border Grid.Column="1"
                                Background="Transparent"
                                MouseLeftButtonDown="TitleBar_OnMouseLeftButtonDown"/>

                        <StackPanel Grid.Column="2"
                                    Orientation="Horizontal"
                                    VerticalAlignment="Center">

                            <Button Style="{StaticResource NewsTopButtonStyle}"
                                    ToolTip="Новости"
                                    Click="OpenNewsTab_OnClick">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock FontFamily="Segoe MDL2 Assets"
                                               Text="&#xE8F2;"
                                               FontSize="14"
                                               Foreground="{StaticResource TextDimLocal}"
                                               Margin="0,0,7,0"/>
                                    <TextBlock Text="Новости"
                                               Foreground="{StaticResource TextStrongLocal}"
                                               VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>

                            <Button Style="{StaticResource ProfileButton}"
                                    Margin="0,0,8,0"
                                    Visibility="{Binding IsLoggedIn, Converter={StaticResource BoolToVis}}"
                                    Command="{Binding OpenProfileCommand}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="8"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Width="25" Height="25"
                                            CornerRadius="9"
                                            ClipToBounds="True"
                                            Background="#24324B"
                                            BorderBrush="#57377F"
                                            BorderThickness="1">
                                        <Grid>
                                            <TextBlock Text="{Binding UserInitial}"
                                                       FontWeight="Bold"
                                                       FontSize="11"
                                                       Foreground="{StaticResource TextStrongLocal}"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"/>
                                            <Image Source="{Binding AvatarUrl, IsAsync=True}"
                                                   Stretch="UniformToFill"/>
                                        </Grid>
                                    </Border>

                                    <TextBlock Grid.Column="2"
                                               Text="{Binding SiteUserName}"
                                               Foreground="{StaticResource TextStrongLocal}"
                                               VerticalAlignment="Center"/>

                                    <TextBlock Grid.Column="3"
                                               Margin="8,0,0,0"
                                               Foreground="{StaticResource TextDimLocal}"
                                               Text="▾"
                                               VerticalAlignment="Center"/>
                                </Grid>

                                <Button.ContextMenu>
                                    <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                                        <MenuItem Header="{Binding Rezonite, StringFormat=Резонит: {0:N0}}"
                                                  IsEnabled="False"/>
                                        <Separator/>
                                        <MenuItem Header="Профиль" Command="{Binding OpenProfileCommand}"/>
                                        <Separator/>
                                        <MenuItem Header="Выйти" Command="{Binding SiteLogoutCommand}"/>
                                    </ContextMenu>
                                </Button.ContextMenu>
                            </Button>

                            <Button Style="{StaticResource WinCtlButton}"
                                    ToolTip="Настройки"
                                    Click="OpenSettingsTab_OnClick">
                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE713;" FontSize="14"/>
                            </Button>

                            <Button Style="{StaticResource WinCtlButton}"
                                    ToolTip="Свернуть"
                                    Click="Minimize_OnClick">
                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE921;" FontSize="14"/>
                            </Button>

                            <Button Style="{StaticResource WinCtlButton}"
                                    ToolTip="Закрыть"
                                    Click="Close_OnClick">
                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE8BB;" FontSize="14"/>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>

                <TabControl x:Name="MainTabs"
                            Grid.Row="2"
                            Style="{StaticResource HiddenTabsStyle}"
                            Focusable="False"
                            KeyboardNavigation.TabNavigation="None"
                            SelectedIndex="{Binding SelectedMenuIndex, Mode=TwoWay}">
                    <TabItem><tabs:AuthTabView/></TabItem>
                    <TabItem><tabs:StartTabView/></TabItem>
                    <TabItem><tabs:ProfileTabView/></TabItem>
                    <TabItem><tabs:SettingsTabView/></TabItem>
                    <TabItem><tabs:NewsTabView/></TabItem>
                </TabControl>
            </Grid>
        </Grid>
    </Border>
</Window>
